diff --git a/quickjs/quickjs.c b/quickjs/quickjs.c
--- a/quickjs/quickjs.c
+++ b/quickjs/quickjs.c
@@ -332,2 +332,4 @@
     JSValue cur_func; /* current function, JS_UNDEFINED if the frame is detached */
+    JSValue capture_this; /* borrowed from the live call or async state */
+    BOOL capture_constructor;
     JSValue *arg_buf; /* arguments */
@@ -458,2 +460,3 @@
     JSRuntime *rt;
+    BOOL stack_formatting;
     struct list_head link;
@@ -17593,2 +17595,4 @@
     sf->cur_func = (JSValue)func_obj;
+    sf->capture_this = this_obj;
+    sf->capture_constructor = !!(flags & JS_CALL_FLAG_CONSTRUCTOR);
     sf->arg_count = argc;
@@ -17839,2 +17843,4 @@
     sf->cur_func = (JSValue)func_obj;
+    sf->capture_this = this_obj;
+    sf->capture_constructor = !!(flags & JS_CALL_FLAG_CONSTRUCTOR);
     var_refs = p->u.func.var_refs;
@@ -20729,2 +20735,4 @@
     s->this_val = JS_DupValue(ctx, this_obj);
+    sf->capture_this = s->this_val;
+    sf->capture_constructor = FALSE;
     s->argc = argc;
@@ -41399,2 +41407,5 @@
+#include "qjs-stacktrace.h"
 static const JSCFunctionListEntry js_error_funcs[] = {
+    JS_CFUNC_DEF("captureStackTrace", 2, qjs_capture_stack_trace),
+    JS_PROP_INT32_DEF("stackTraceLimit", 10, JS_PROP_WRITABLE | JS_PROP_CONFIGURABLE),
     JS_CFUNC_DEF("isError", 1, js_error_isError),
